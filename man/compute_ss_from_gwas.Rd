% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ss_from_gwas.R
\name{compute_ss_from_gwas}
\alias{compute_ss_from_gwas}
\title{Compute regression sufficient statistics from GWAS results}
\usage{
compute_ss_from_gwas(
  beta,
  se,
  LD,
  n,
  response_var = NULL,
  scale = c("auto", "standardized", "original"),
  residual_df = n - 2,
  output = c("sufficient", "eigen")
)
}
\arguments{
\item{beta}{A finite numeric vector of marginal ordinary least-squares
effect estimates.}

\item{se}{A positive finite numeric vector of standard errors corresponding
to \code{beta}.}

\item{LD}{A finite, symmetric LD correlation matrix whose rows and columns
have the same order and effect-allele orientation as \code{beta}. Dense base-R
matrices and compressed sparse \code{Matrix} objects of class \code{dgCMatrix} or
\code{dsCMatrix} are supported. Supply signed correlations, not squared
correlations.}

\item{n}{Number of observations used to calculate the GWAS statistics.}

\item{response_var}{Optional positive sample variance of the response. It is
required for \code{scale = "original"}.}

\item{scale}{Scale on which to construct the sufficient statistics.
\code{"auto"} uses the original coefficient scale when \code{response_var} is
supplied and otherwise standardizes both predictors and response.}

\item{residual_df}{Positive residual degrees of freedom used by the marginal
regressions. The SuSiE-RSS ordinary least-squares conversion uses the
default \code{n - 2}.}

\item{output}{Whether to return cross-products for \code{\link[=blm_ss]{blm_ss()}} or a complete
raw eigendecomposition for preparing input to \code{\link[=blm_ss_eigen]{blm_ss_eigen()}}.}
}
\value{
For \code{output = "sufficient"}, a list containing \code{n}, \code{XtX}, \code{Xty},
\code{yty}, zero \code{X_means} and \code{y_mean}, and \code{reference_response_var}. Pass
\code{reference_response_var} to \code{\link[=blm_ss]{blm_ss()}} only when at least one \code{ETA} block
specifies \code{expected_pve}. For \code{output = "eigen"}, \code{XtX} is replaced by
\code{XtX_eigenvectors_raw}, \code{XtX_eigenvalues_raw}, and
\code{XtX_eigenvalue_tolerance}. Raw eigenpairs are returned without filtering
or modification and therefore are not necessarily valid inputs to
\code{\link[=blm_ss_eigen]{blm_ss_eigen()}}, which requires strictly positive eigenvalues.
}
\description{
Reconstructs centered linear-regression cross-products from marginal GWAS
effect estimates, their standard errors, and a signed LD correlation matrix
using the finite-sample transformation employed by SuSiE-RSS.
}
\details{
Let \eqn{z_j=\hat b_j/\hat s_j} and
\deqn{a_j=(n-1)/(z_j^2+\nu),}
where \eqn{\nu} is \code{residual_df}. On the standardized scale, the function
returns
\deqn{X'X=(n-1)R,\quad
      X'y=\sqrt{n-1}\,\sqrt{a}\,z,\quad y'y=n-1.}
When \code{response_var} is supplied and the original scale is requested, it
sets
\deqn{d_j=response\_var\,a_j/\hat s_j^2,}
\deqn{X'X=D^{1/2}RD^{1/2},\quad X'y=D\hat b,\quad
      y'y=(n-1)response\_var.}

These are exact centered cross-products when the marginal statistics are
ordinary least-squares results from a common sample and \code{LD} is the
corresponding in-sample predictor correlation matrix. With reference LD,
meta-analysis statistics, variable per-variant sample sizes, mixed-model
statistics, or other non-OLS inputs, they are approximate working
cross-products. In particular, learning the residual variance is generally
safer with in-sample LD; fixing it is generally safer with reference LD.

The function performs only basic input validation. It does not diagnose
allele flips or broader incompatibility between the GWAS statistics and
LD. Eigen output uses a complete dense eigendecomposition, requiring
\eqn{O(p^2)} memory and \eqn{O(p^3)} time. Every eigenpair is returned,
including zero and negative eigenvalues. A warning is produced if any
negative eigenvalues are present. Users must choose the eigenpairs passed
to \code{\link[=blm_ss_eigen]{blm_ss_eigen()}} and calculate the corresponding \code{XtX_prop_var}.
}
\examples{
set.seed(1)
X <- scale(matrix(rnorm(200), 40, 5), center = TRUE, scale = FALSE)
y <- drop(scale(rnorm(40), center = TRUE, scale = FALSE))
marginal_beta <- drop(crossprod(X, y)) / colSums(X^2)
marginal_se <- sqrt(
  (sum(y^2) - marginal_beta^2 * colSums(X^2)) /
    ((nrow(X) - 2) * colSums(X^2))
)
ss <- compute_ss_from_gwas(
  marginal_beta, marginal_se, stats::cor(X), nrow(X),
  response_var = stats::var(y)
)
}
\references{
Zou Y, Carbonetto P, Wang G, Stephens M (2022). Fine-mapping from summary
data with the "Sum of Single Effects" model. PLoS Genetics 18:e1010299.
}
