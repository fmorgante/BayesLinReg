% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blm_gwas.R
\name{blm_gwas}
\alias{blm_gwas}
\title{Bayesian linear regression from GWAS summary statistics and LD}
\usage{
blm_gwas(
  gwas,
  ld,
  ETA,
  residual_var = NULL,
  residual_shape = NULL,
  residual_scale = NULL,
  reference_response_var = NULL,
  scale = c("auto", "standardized", "original"),
  residual_df_gwas = NULL,
  ld_shrink = 0,
  iterations = 4000L,
  burnin = 1000L,
  thin = 1L,
  verbose = FALSE,
  nchains = 1L,
  nthreads = 1L,
  store_samples = FALSE,
  store_coefficient_cov = FALSE,
  check_psd = FALSE,
  compute_pve = FALSE,
  pve_type = c("standalone", "allocated")
)
}
\arguments{
\item{gwas}{A data frame with columns \code{CHR}, \code{ID}, \code{POS}, \code{A1}, \code{A0}, \code{N},
\code{BETA}, and \code{SE}. \code{BETA} is the additive marginal effect per \code{A1} allele.
\code{N} may vary among retained variants; noninteger effective sample sizes
are allowed.}

\item{ld}{A \code{blm_ld} object returned by \code{\link[=as_blm_ld]{as_blm_ld()}} or a
\code{blm_ld_eigen} object returned by \code{\link[=as_blm_ld_eigen]{as_blm_ld_eigen()}}.}

\item{ETA}{Prior specifications in the same format as \code{\link[=blm_ss]{blm_ss()}}. Character
\code{indices} refer to variant IDs.}

\item{residual_var, residual_shape, residual_scale}{Residual-variance controls
with the same meaning and defaults as in \code{\link[=blm_ss]{blm_ss()}}.}

\item{reference_response_var}{Reference response variance used for original-
scale reconstruction and expected-PVE calibration. It has the same prior-
calibration meaning as in \code{\link[=blm_ss]{blm_ss()}}.}

\item{scale}{Working scale. \code{"auto"} uses the original response scale when
\code{reference_response_var} is supplied and otherwise standardizes the
response and predictors. \code{"original"} requires
\code{reference_response_var}.}

\item{residual_df_gwas}{Positive residual degrees of freedom used by the
marginal GWAS regressions. It may be a scalar or one value per input GWAS
row. The default is \code{N - 2} elementwise.}

\item{ld_shrink}{Numeric scalar in \verb{[0, 1)}. Off-diagonal LD correlations
are multiplied by \code{1 - ld_shrink} while the unit diagonal is retained.
This is applied by the native LD operator without copying or modifying
\code{ld}. Positive values can stabilize analyses using external LD. It is not
available for \code{blm_ld_eigen} input because the eigen object already
represents an explicit LD approximation.}

\item{iterations, burnin, thin}{MCMC controls; see \code{\link[=blm_ss]{blm_ss()}}.}

\item{verbose, nchains, nthreads}{Progress, chain, and within-chain block
parallelism controls; see \code{\link[=blm_ss]{blm_ss()}}.}

\item{store_samples, store_coefficient_cov}{Posterior-storage controls; see
\code{\link[=blm_ss]{blm_ss()}}.}

\item{check_psd}{Whether to perform the optional blockwise PSD and joint
compatibility validation; see \code{\link[=blm_ss]{blm_ss()}}.}

\item{compute_pve, pve_type}{Posterior PVE controls; see \code{\link[=blm_ss]{blm_ss()}}.}
}
\value{
An object of class \code{blm_fit}. Coefficients are oriented to the input
GWAS \code{A1} alleles. No intercept is fitted because centered GWAS summary
statistics do not identify the phenotype mean. Nevertheless, their
centered likelihood has \code{likelihood_df = N - 1}; this value is recorded
in the result and is separate from \code{residual_df_gwas}.
\code{gwas_sample_size_summary} describes retained \code{N}, and \code{gwas_likelihood}
identifies the likelihood construction. With heterogeneous \code{N}, the fit
also records \code{gwas_reference_n} and \code{gwas_sample_overlap_assumption}, and
it has no single \code{likelihood_df}. When \code{ld} was created by
\code{\link[=regularize_blm_ld]{regularize_blm_ld()}}, \code{ld_regularization_report} preserves that object's
original regularization audit report unchanged, while
\code{ld_regularization_block_map} maps fitted post-harmonization blocks to the
source blocks in that report. Internally, \code{source_block} retains the
original regularization source and \code{subset_source_block} records the
immediate parent across repeated filtering stages. \code{ld_harmonization} reports
retained and flipped variants together with separate counts for GWAS-only,
LD-only, location-mismatched, allele-mismatched, and ambiguous variants.
Its \code{excluded} element counts excluded table entries: unmatched entries
contribute one and matched-but-incompatible variant pairs contribute two.
Eigen LD fits additionally report \code{ld_eigen_rank}, block-specific ranks
and retained trace fractions, aggregate \code{ld_prop_var}, and whether the
representation is approximate. Trace fractions use the unit-diagonal
trace under the strict eigenvalue policy and the available positive trace
under \code{negative_eigenvalues = "discard"}.
}
\description{
Fits the same coefficient-prior models as \code{\link[=blm_ss]{blm_ss()}} from additive
quantitative-trait GWAS results and a reusable LD object created by
\code{\link[=as_blm_ld]{as_blm_ld()}} or its truncated-eigen analogue \code{\link[=as_blm_ld_eigen]{as_blm_ld_eigen()}}. The
working Gram matrix is applied without materializing a global dense matrix.
}
\details{
Variants are matched by \code{ID} and checked against chromosome,
position, and alleles. Reversed alleles are handled by changing effect
orientation. With native LD, unmatched, incompatible, and unresolved
strand-ambiguous variants are excluded with a warning. Excluded IDs are
also removed from character-indexed \code{ETA} blocks, with a block-specific
warning; a block that becomes empty is rejected. If harmonization excludes
an LD variant, numeric \code{ETA} indices are rejected because their intended
position is ambiguous; use character variant IDs instead. Eigen LD instead
requires complete compatible coverage of its LD variant panel. Gibbs
coordinates retain LD order, while \code{ETA} blocks remain independent of LD
blocks.

A \code{blm_ld_eigen} object uses the pure truncated representation
\eqn{R_q=U_q\Lambda_qU_q'} stored in that object. The omitted eigenspace
is treated as having zero LD variance; no diagonal correction is added.
Eigen LD requires complete coverage of its variant panel. GWAS rows may be
reordered and may contain additional variants, but every eigen-LD variant
must have compatible GWAS position and allele metadata. Match a native
\code{blm_ld} object with \code{\link[=match_gwas_ld]{match_gwas_ld()}} before eigen decomposition when
variants need to be removed.
Predictor standardization and expected-PVE prior calibration continue to
use the original unit-diagonal LD scale, so changing \code{prop_var} does not
silently redefine prior scales.

With \code{scale = "standardized"}, the working statistics use
\verb{XtX = (N - 1) R} and response variance one. With \code{scale = "original"},
predictor cross-product diagonals are reconstructed from \code{BETA}, \code{SE},
\code{residual_df_gwas}, and \code{reference_response_var}. Reference-panel LD and
GWAS results not obtained by common-sample ordinary least squares define
approximate working sufficient statistics.
The centered working likelihood uses \code{N - 1} degrees of freedom for the
residual inverse-gamma update and PVE normalization even though no
intercept can be returned from the summary statistics.

When LD was estimated outside the GWAS sample, fixing \code{residual_var} is
recommended. Learning it requires the reconstructed \code{XtX}, \code{Xty}, and
\code{yty} to be mutually compatible; reference-panel mismatch can violate
that requirement. For standardized quantitative traits,
\code{residual_var = 1} is a conservative robust choice. \code{ld_shrink} addresses
LD regularization but does not make approximate statistics exact.

When \code{N} differs among variants (or contains noninteger effective sample
sizes), \code{blm_gwas()} uses a heterogeneous-sample-size RSS approximation.
Each marginal statistic receives its own likelihood scaling, while the
score-correlation matrix is assumed to equal the supplied LD matrix. This
assumes essentially common sample overlap; marginal \code{N} values alone do
not identify pairwise sample overlap. The residual variance must be fixed
at one and \code{check_psd = TRUE} is unavailable in this mode. PVE continues
to use predictor covariance rather than likelihood information, and a
separate PVE scaling is applied internally. The median \code{N}, rounded to an
integer, is used only as a computational and GlobalLocal prior-calibration
reference.
}
