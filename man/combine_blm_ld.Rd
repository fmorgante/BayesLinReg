% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ld_matrix.R
\name{combine_blm_ld}
\alias{combine_blm_ld}
\title{Combine LD matrix objects}
\usage{
combine_blm_ld(...)
}
\arguments{
\item{...}{One or more \code{blm_ld} objects, or one nonempty list of
\code{blm_ld} objects. A single \code{blm_ld} object is returned unchanged.}
}
\value{
A \code{blm_ld} object containing all input variants and computational blocks
in input order. If any input carries a \code{regularization_report}, the
combined report contains all blocks; unregularized inputs receive
\code{method = "none"} with unavailable numerical diagnostics.
}
\description{
Combines independently constructed \code{blm_ld} objects without materializing
or recompressing their LD matrices. Cross-object LD is assumed to be exactly
zero, just as it is between matrices supplied in a list to
\code{\link[=as_blm_ld]{as_blm_ld()}}.
}
\details{
Parent names, computational block names, and variant IDs must be unique across
inputs. Variant metadata tables must have identical column names. The
compressed numerical vectors are reused through R's copy-on-modify semantics;
the function allocates combined list metadata, a combined variant table, and a
block table, but does not reconstruct any correlation matrix.
}
