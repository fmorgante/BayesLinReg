% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/blm_ss.R
\name{blm_ss}
\alias{blm_ss}
\title{Bayesian linear regression from sufficient statistics}
\usage{
blm_ss(
  n,
  XtX,
  Xty,
  ETA,
  yty = NULL,
  X_means = NULL,
  y_mean = NULL,
  residual_var = NULL,
  residual_shape = NULL,
  residual_scale = NULL,
  reference_response_var = NULL,
  iterations = 4000L,
  burnin = 1000L,
  thin = 1L,
  version = c("Rcpp", "R"),
  verbose = FALSE,
  nchains = 1L,
  nthreads = 1L,
  store_samples = FALSE,
  store_coefficient_cov = FALSE,
  check_psd = FALSE,
  XtX_storage = c("auto", "speed", "memory"),
  XtX_memory_limit = 1024^3,
  compute_pve = FALSE,
  pve_type = c("standalone", "allocated"),
  likelihood_df = NULL
)
}
\arguments{
\item{n}{Number of observations used to form the sufficient statistics.}

\item{XtX}{A finite, symmetric predictor cross-product matrix, or a nonempty
list of such matrices representing an exactly block-diagonal
cross-product. Dense base-R matrices and compressed sparse \code{Matrix}
objects of class \code{dgCMatrix} or \code{dsCMatrix} are supported. List and sparse
inputs require \code{version = "Rcpp"}.}

\item{Xty}{A finite predictor-response cross-product vector.}

\item{ETA}{A prior specification or named list of prior blocks. Each block
must contain \code{model} and may contain \code{indices}, an integer or character
vector selecting columns of \code{XtX}. A single block may omit \code{indices} and
then uses every predictor. Multiple blocks must partition all predictors.
Prior parameters and \code{standardize} are the same as in \code{\link[=blm]{blm()}}.}

\item{yty}{Optional finite, nonnegative response sum of squares. It is
required when \code{residual_var = NULL}.}

\item{X_means, y_mean}{Optional predictor means and response mean. They must
be supplied together. When omitted, the model is fitted without an
intercept.}

\item{residual_var}{A positive known residual variance, or \code{NULL} to learn
it using an inverse-gamma prior.}

\item{residual_shape, residual_scale}{Positive shape and scale parameters for
the inverse-gamma residual-variance prior. \code{residual_shape} is required
when \code{residual_var = NULL}. \code{residual_scale} is also required unless every
\code{ETA} block supplies \code{expected_pve}, in which case omitting it calibrates
its value from the response variance not assigned to the blocks.}

\item{reference_response_var}{Optional positive reference response variance
used to calibrate blocks that specify \code{expected_pve}. When omitted, it is
recovered from \code{yty} and \code{y_mean}; if that is not possible, it must be
supplied explicitly.}

\item{iterations}{Total Gibbs iterations when sampling is required.}

\item{burnin}{Number of initial iterations to discard.}

\item{thin}{Interval between retained draws.}

\item{version}{Gibbs implementation: \code{"Rcpp"} or \code{"R"}.}

\item{verbose}{If \code{TRUE}, display aggregate progress at 10-percent intervals
per chain using \code{\link[progressr:with_progress]{progressr::with_progress()}}.}

\item{nchains}{Number of independent chains. Multiple chains use a temporary
\link[future:multisession]{future::multisession} plan.}

\item{nthreads}{Number of threads used within one Rcpp chain for list \code{XtX}.
Values greater than one require \code{nchains = 1} and zero working predictor
means. The default preserves the serial sampler and its RNG sequence.}

\item{store_samples}{If \code{TRUE}, retain and return individual posterior draws.
The default, \code{FALSE}, computes posterior summaries online without
allocating draw matrices. Fits without stored samples cannot be passed to
\code{\link[=assess_convergence]{assess_convergence()}}.}

\item{store_coefficient_cov}{If \code{TRUE}, return the full posterior coefficient
covariance matrix for each \code{ETA} block. The default, \code{FALSE}, returns only
the named vector of marginal coefficient variances and avoids blockwise
quadratic-size covariance accumulators when samples are not stored.}

\item{check_psd}{If \code{TRUE}, use a full eigendecomposition to verify that the
centered \code{XtX} is positive semidefinite and that \code{Xty} and \code{yty} are
jointly compatible with it. The default, \code{FALSE}, avoids this
\eqn{O(p^3)} validation cost. Symmetry and basic input checks are always
performed. Sparse or list \code{XtX} is materialized as one dense matrix for
this optional validation.}

\item{XtX_storage}{Storage policy for symmetric sparse matrices, supplied
either directly or within list input. \code{"speed"} expands them to general
sparse matrices, \code{"memory"} chooses the smaller exact representation, and
\code{"auto"} expands them only while the estimated total representation
remains within \code{XtX_memory_limit}.}

\item{XtX_memory_limit}{Positive approximate byte limit used by
\code{XtX_storage = "auto"}. It controls internal Gram storage, not posterior
draws or other fit allocations.}

\item{compute_pve}{If \code{TRUE}, calculate block and total proportions of
variance explained at retained posterior draws. The default, \code{FALSE},
adds no PVE computation to the sampler.}

\item{pve_type}{Definition used for block PVE. \code{"standalone"} divides each
block's fitted-value variance by total fitted variance plus residual
variance. \code{"allocated"} additionally allocates cross-block covariance so
that block PVE values sum to total PVE. It is used only when
\code{compute_pve = TRUE}.}

\item{likelihood_df}{Optional positive integer no greater than \code{n}, giving
the likelihood dimension used in residual-variance, PVE, and
\code{expected_pve} calibration calculations.
The default is \code{n - 1} when \code{X_means} and \code{y_mean} identify an intercept,
and \code{n} otherwise. For centered sufficient statistics supplied without
means, set \code{likelihood_df = n - 1} explicitly.}
}
\value{
A fitted object with the same block-specific posterior summaries as
\code{\link[=blm]{blm()}}. Intercept components are present only when both \code{X_means} and
\code{y_mean} are supplied. For sparse \code{XtX}, the result records the selected
storage representation. For list \code{XtX}, it also records the block-diagonal
assumption and block sizes.
}
\description{
Fits the same prior models as \code{\link[=blm]{blm()}} directly from cross-products instead
of the original response and predictor matrix.
}
\details{
If means are supplied, \code{XtX}, \code{Xty}, and \code{yty} are interpreted as
uncentered cross-products and are centered internally. Without means,
they are used as supplied. If any block requests standardization without
means, a warning reminds the user that the supplied cross-products should
already represent centered or standardized variables.
For list \code{XtX}, predictor order is the concatenation of the matrices in
list order and every omitted cross-block product is assumed to be exactly
zero. Gram blocks and \code{ETA} prior blocks are independent: either may split
or span the other. When predictor names are used, every Gram block must
provide matching row and column names that are unique across the list.
Centering is still exact because its global dense rank-one correction is
maintained separately.
When \code{expected_pve} is used and \code{reference_response_var} is omitted, the
response variance is recovered from \code{yty} and \code{y_mean}; otherwise an
explicit positive \code{reference_response_var} is required.
When every block supplies \code{expected_pve}, omitting \code{residual_scale}
calibrates it from their total expected PVE exactly as in \code{\link[=blm]{blm()}}.
A \code{"Fixed"} block uses a flat prior and is jointly rank-checked with every
other fixed block using the centered and standardized fixed-predictor
submatrix of \code{XtX}. This check is always performed and is separate from
the optional full-matrix \code{check_psd} validation.
Optional posterior PVE calculations use the centered cross-products and
the definitions in \code{\link[=blm]{blm()}}. Their cost is quadratic in block size for a
dense \code{XtX} and proportional to the relevant stored entries for sparse
input, at each retained draw.

Coefficients are sampled with a right-hand-side update that maintains
\eqn{X'y-X'X\beta}; individual-level pseudo-observations are not formed.
For sparse \code{XtX}, the Rcpp sampler traverses only stored entries.
\code{XtX_storage} can expand a symmetric \code{dsCMatrix} once to a general
\code{dgCMatrix}, or retain a lower triangle and stream each ascending Gibbs
sweep without a reverse adjacency index. This applies to both direct and
list input. The full right-hand-side state is reconstructed once after
each streaming sweep. The dense rank-one centering correction is
maintained separately rather than materialized.
With list input and zero working predictor means, \code{nthreads > 1} updates
separate Gram blocks concurrently through \code{RcppParallel}. Updates remain
sequential within each Gram block. Shared prior hyperparameters and the
residual variance are updated after the parallel coefficient sweep. For
GlobalLocal blocks, the conditionally independent local-variance GIG
updates also run concurrently across Gram blocks.
After \code{\link[=set.seed]{set.seed()}}, threaded runs are reproducible and use a different RNG
stream from the serial sampler.
Set \code{check_psd = TRUE} when the supplied statistics are not known to come
from a valid common data matrix and response vector.
When residual variance is learned, the sampler also checks reconstructed
residual SSE values during its existing periodic state refresh and whenever
an incremental value becomes suspiciously negative. Values within a
scale-aware floating-point tolerance are clamped to zero; materially
negative values produce an incompatibility error. This runtime guard is
inexpensive but does not prove that \code{XtX} is positive semidefinite, so it
does not replace \code{check_psd = TRUE}.
}
\examples{
X <- cbind(x1 = 1:20, x2 = rep(c(0, 1), 10))
y <- 1 + 2 * X[, "x1"] - X[, "x2"]
set.seed(123)
fit <- blm_ss(
  n = nrow(X),
  XtX = crossprod(X),
  Xty = drop(crossprod(X, y)),
  ETA = list(model = "Normal"),
  yty = sum(y^2),
  X_means = colMeans(X),
  y_mean = mean(y),
  residual_var = 1,
  iterations = 100,
  burnin = 50
)
}
