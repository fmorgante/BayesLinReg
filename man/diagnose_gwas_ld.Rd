% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gwas_ld_diagnostics.R
\name{diagnose_gwas_ld}
\alias{diagnose_gwas_ld}
\title{Diagnose variant-level consistency between GWAS statistics and LD}
\usage{
diagnose_gwas_ld(
  gwas,
  ld,
  window_variants = 2000L,
  overlap_variants = 200L,
  ld_shrink = 0.05,
  p_threshold = 5e-08,
  min_tagging = 0.1,
  conditional_variance_floor = sqrt(.Machine$double.eps),
  n_variation_tolerance = 0.1,
  nthreads = 1L,
  store_variant_report = c("failures", "all")
)
}
\arguments{
\item{gwas}{A GWAS summary-statistics object accepted by \code{\link[=blm_gwas]{blm_gwas()}}.}

\item{ld}{A native \code{blm_ld} object returned by \code{\link[=as_blm_ld]{as_blm_ld()}}. Diagnostics
should normally be run before conversion to \code{blm_ld_eigen}.}

\item{window_variants}{Maximum number of central variants assigned to one
diagnostic window.}

\item{overlap_variants}{Number of neighboring variants added on either side
of each central window for prediction.}

\item{ld_shrink}{Numeric scalar in \verb{[0, 1)}. The diagnostic covariance is
\code{(1 - ld_shrink) * R + ld_shrink * I}. A positive value stabilizes the
conditional solves without modifying \code{ld}.}

\item{p_threshold}{Conditional-discrepancy p-value threshold used to flag
variants.}

\item{min_tagging}{Minimum conditional tagging strength required before a
variant can be flagged statistically.}

\item{conditional_variance_floor}{Positive numerical floor for conditional
variances.}

\item{n_variation_tolerance}{Nonnegative relative range allowed within an LD
block before its sample sizes are marked dissimilar.}

\item{nthreads}{Number of threads used to process independent diagnostic
windows. When the package uses external BLAS, values greater than one
require the applicable BLAS thread setting to be explicitly equal to one.}

\item{store_variant_report}{Whether to retain only statistically flagged
variants or the complete per-variant diagnostic table.}
}
\value{
An object of class \code{blm_gwas_ld_diagnostics} containing
\code{block_report}, \code{variant_failures}, optionally \code{variant_report}, the
harmonization counts, controls, and the score-correlation assumption.
}
\description{
Performs a read-only, blockwise conditional z-score diagnostic after the
same position and allele harmonization used by \code{\link[=match_gwas_ld]{match_gwas_ld()}}. No variant
is removed and no allele is changed based on a statistical diagnostic.
}
\details{
Let \code{z} be oriented to the LD dosage allele. Within each overlapping
window, variants are randomly divided into two balanced groups. Each group
is predicted from the other. For target variant \code{j} and predictor set \code{t},
the diagnostic calculates
\verb{predicted_z = R[j,t] solve(R[t,t], z[t])} and divides the squared residual
by \verb{1 - R[j,t] solve(R[t,t], R[t,j])}. The supplied \code{ld_shrink} is applied
to all these covariance terms. Set the R seed with \code{\link[=set.seed]{set.seed()}} to make the
random partitions reproducible.

This is a scalable, conservative DENTIST-style first-pass diagnostic, not
a reimplementation of the complete iterative DENTIST procedure. Windows
are bounded by variant count, processed sequentially, and linear solves
are batched so a global dense LD matrix is never created. Optimized BLAS
can accelerate the Cholesky and triangular-solve operations.

Conditional p-values assume that correlations among GWAS z-scores equal
the supplied reference LD. Marginal sample sizes do not identify pairwise
sample overlap. Blocks exceeding \code{n_variation_tolerance} are marked in
\code{block_report}, but their diagnostics are still returned as sensitivity
measures and should not be used for automatic filtering.

The external-BLAS guard checks the documented vendor environment settings
present in the R process. Vendor-specific calls that change BLAS threads
after R starts cannot be detected portably and should not be used together
with code{nthreads > 1}.

\code{possible_allele_flip} is only statistical evidence: alleles should be
changed automatically only when variant metadata establish the change.
To filter explicitly, remove selected IDs from \code{gwas} and then call
\code{\link[=match_gwas_ld]{match_gwas_ld()}} to construct the corresponding native LD subset.
}
\examples{
R <- matrix(c(1, 0.95, 0.95, 1), 2)
variants <- data.frame(
  CHR = 1, ID = c("rs1", "rs2"), POS = 1:2,
  A1 = c("A", "A"), A0 = c("C", "C")
)
ld <- as_blm_ld(R, variants)
gwas <- transform(
  variants, N = 1000, BETA = c(0.3, -0.3), SE = 0.05
)
set.seed(1)
qc <- diagnose_gwas_ld(gwas, ld, window_variants = 2)
qc$variant_failures
}
